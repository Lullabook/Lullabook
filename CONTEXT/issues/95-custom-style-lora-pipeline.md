# 95 — Custom art-style trained Style-LoRA pipeline (Plus)

Status: cut

Spec'd the Plus-tier custom art style: a durable fal Style-LoRA training pipeline (same
shape as the persona LoRA, ADR-0002) selectable as a book's Style Bible (ADR-0012),
metered via issue 94, with train-failure falling back to the default Style Bible +
credit refund, and hard-delete purging the Family-scoped Style-LoRA blob.
Explicitly named in the PRD v16 R2-defer list (149-dead-ui-endpoint-sweep.md) alongside
video pages — deferred/cut from R1, kept behind config for R2, endpoint must return
clean 404/403, never run.

(condensed 2026-07-07 — full spec in git history)
