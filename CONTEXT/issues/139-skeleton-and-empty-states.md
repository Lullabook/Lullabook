# 139 — Skeleton loaders + illustrated empty states
Status: shipped
Reusable shimmer `Skeleton` card (mirrors final layout) replacing bare `ActivityIndicator` loading branches; illustrated empty states (large emoji + CTA) replacing gray one-line text.
Naming: the `Skeleton` + empty-state components are reused by 142 (native lists) for roster/library/journal loading + empty states.
(condensed 2026-07-07 — full spec in git history)
