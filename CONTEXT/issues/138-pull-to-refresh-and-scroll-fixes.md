# 138 — Pull-to-refresh + remove Refresh button + scroll fixes
Status: shipped
Added `RefreshControl` to the `Screen` scroll view (home/stories/family/daily); deleted the literal "↻ Refresh" button. Fixed the nested `ScrollView`-in-`Screen` on the billing paywall that broke scroll/momentum. Bumped `BackPill` to a 44pt hit target.
(condensed 2026-07-07 — full spec in git history)
