# 24 — Free Character tier: Trait Questionnaire + text Story + native reader

Status: shipped

Free, non-gated tier: Character via Trait Questionnaire (no photos/biometric/LoRA), text-only Story (Bedtime/Learning), native reading view. Real-child Characters need a light attestation per Jurisdiction; fully-fictional Characters skip consent entirely. Binding invariant: text generation is never gated on subscription. Also fixed a fail-open moderation bug (non-numeric score must fail closed) — that fail-closed rule still binds.

(condensed 2026-07-07 — full spec in git history)
